const router = require('express').Router();
const service = require('./user.service');

// public api routes
// router.get('/manageUser', service.manageUser);

// router.get('/register', service.registerUser);

module.exports = router